{
  "name": "fortdefend-agent",
  "version": "1.0.0",
  "main": "agent.js",
  "scripts": {
    "build": "pkg agent.js --target node18-win-x64 --output dist/FortDefendAgent.exe --compress GZip",
    "build:installer": "node build-installer.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  },
  "pkg": {
    "assets": [
      "*.json"
    ],
    "targets": [
      "node18-win-x64"
    ]
  }
}
